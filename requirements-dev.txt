# Dev/test tooling, on top of backend/requirements.txt + ml/requirements.txt.
#   pip install -r requirements-dev.txt
pytest==8.3.4
httpx==0.28.1          # FastAPI TestClient transport
ruff==0.8.4            # lint + import sort
