# QuantML ML pipeline deps (ingestion -> features -> training -> inference).
pandas==2.2.3
numpy==2.1.3
scikit-learn==1.6.0
xgboost==2.1.3
yfinance==0.2.51
pyarrow==18.1.0
pydantic-settings==2.7.1
optuna==4.1.0          # walk-forward hyperparameter search (ml/training/tune.py)

# Future:
# lightgbm        # alternative GBM
# mlflow          # experiment tracking / model registry
# shap            # (we use xgboost native pred_contribs, so not required)
